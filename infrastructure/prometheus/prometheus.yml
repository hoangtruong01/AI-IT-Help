# ==============================
# EOMP — Prometheus Configuration
# ==============================

global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  # Prometheus self-monitoring
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]

  # Gateway service (will be enabled when service is running)
  # - job_name: "gateway"
  #   static_configs:
  #     - targets: ["gateway:8080"]
  #   metrics_path: /metrics
